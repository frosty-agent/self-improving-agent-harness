#!/usr/bin/env sh
# Generate the deterministic, redacted JSON and HTML run reports in Docker.
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" --writable-workspace --no-network --non-interactive \
  --load scripts/run-report.lisp
