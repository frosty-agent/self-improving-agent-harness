#!/usr/bin/env sh
# Execute the harness entry point in the portable container runtime.
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" \
  --non-interactive \
  --load scripts/run-harness.lisp
