#!/usr/bin/env sh
# Run the deterministic structured source-mutation research prototype in Docker.
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_root/bin/container" --writable-workspace --no-network --non-interactive \
  --load scripts/run-source-mutation.lisp
