# Common Lisp build artifacts
*.fasl
*.fsl
*.dfsl
*.lib
*.dx32fsl
*.dx64fsl
*.lx32fsl
*.lx64fsl

# Local configuration and credentials
.env
.env.*
!.env.example
secrets/

# Generated offline run-report artifacts
reports/

# Local chat session diagnostic JSONL (workspace-visible)
agent-logs/

# Python bytecode generated by supervisor invocations
bin/__pycache__/

# Editor and OS artifacts
.DS_Store
*~
.#*

# Codex ChatGPT/Codex OAuth home (issue #18). Contains auth.json with OAuth
# tokens; must never be committed, imaged, or logged.
.codex-home/

# Temporary planning/design documents
docs-tmp/

# Node dependencies for the Playwright browser bridge (src/tooling/browser/).
# Installed on the host; the workspace mounts read-only at runtime.
node_modules/
